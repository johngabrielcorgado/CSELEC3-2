import { Delete } from 'lucide-react'
import React from 'react'

const pending = ({tasks, onToggle, Delete}) => {
    return (
        <div className='task-section'>
            <h2>Pending Tasks</h2>
        
        </div>
    )
}

